alias: 'vacuum_error'
trigger:
  - platform: state
    entity_id: vacuum.xiaomi_vacuum_cleaner
    to: 'error'
action:
  - service: notify.mobiles
    data:
      message: "L'aspirateur est bloqué (\"{{ state_attr('vacuum.xiaomi_vacuum_cleaner', 'error') }}\")"
