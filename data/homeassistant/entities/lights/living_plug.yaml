platform: template
lights:
  living_plug:
    level_template: "{{ state_attr('light.0x90fd9ffffe87490c_light', 'brightness') }}"
    value_template: "{{ states('light.myhomeserver1_salon_prise') }}"
    turn_on:
      service: light.turn_on
      data_template:
        entity_id: light.myhomeserver1_salon_prise
    turn_off:
      service: light.turn_off
      data_template:
        entity_id: light.myhomeserver1_salon_prise
    set_level:
      service: light.turn_on
      data_template:
        entity_id: light.0x90fd9ffffe87490c_light
        brightness: "{{ brightness }}"
